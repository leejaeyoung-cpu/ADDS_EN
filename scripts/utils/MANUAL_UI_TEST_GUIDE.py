"""
Manual UI Integration Test Guide
Phase 1.2 - Physician Notes Workflow

사용자가 직접 수행해야 하는 테스트입니다.
"""

print("""
================================================================================
  📋 MANUAL UI INTEGRATION TEST - Physician Notes Workflow
================================================================================

🎯 목표: 환자 선택 → 소견서 입력 → GPT 파싱 → DB 저장 전체 워크플로우 검증

================================================================================
  ✅ 사전 확인 (이미 완료됨)
================================================================================

1. Backend: http://localhost:8000 ✅
   - GPU: NVIDIA RTX 5070 감지됨
   - Cellpose 초기화 완료
   
2. Streamlit: http://localhost:8505 ✅
   - 이미 실행 중

================================================================================
  📝 테스트 단계 (브라우저에서 직접 수행)
================================================================================

STEP 1: 브라우저에서 UI 열기
─────────────────────────────────────────────────────────────────────────
  http://localhost:8505 
  
  ✓ 페이지가 정상적으로 로드되는지 확인
  ✓ 좌측 사이드바에 "Patient Management" 메뉴 확인


STEP 2: 환자 검색
─────────────────────────────────────────────────────────────────────────
  1. "Patient Management" 클릭
  2. 검색창에 입력: TEST-UI-001
  3. "검색 결과" 확인
  
  ✓ 환자가 표시되는지 확인 (DB ID: 2)
  ✓ 없으면 "신규 환자 등록" 버튼으로 생성


STEP 3: 환자 선택
─────────────────────────────────────────────────────────────────────────
  1. 검색 결과에서 환자 선택
  2. "이 환자 선택" 버튼 클릭
  
  ✓ 화면 상단에 환자 정보 표시:
     "환자: UI Integration Test (TEST-UI-001)"


STEP 4: 병리 정보 입력
─────────────────────────────────────────────────────────────────────────
  섹션 2: 병리 정보 입력
  
  A. 임상 정보
     - 종양 위치: Right colon
     - TNM 병기: T3N1M0
     - MSI 상태: MSS
     - KRAS 돌연변이: WT
     - ECOG 점수: 1
  
  B. 의사 소견서 (중요!)
     다음 텍스트를 복사해서 붙여넣기:
     
     ┌─────────────────────────────────────────────────────────┐
     │ 58세 남성 환자로 복통 및 혈변 호소                      │
     │ 체중 감소 5kg (최근 3개월)                              │
     │ 대장내시경 검사 결과: 우측 대장 선암종 진단             │
     │ 병기: T3N1M0 (Stage IIIB)                               │
     │ ECOG 1점, 일반 상태 양호                                │
     │ 적극적 항암 치료 원함                                   │
     │ 당뇨, 고혈압 기저질환 있음 (약물로 조절 중)             │
     └─────────────────────────────────────────────────────────┘


STEP 5: 폼 제출 및 검증 ⭐ 가장 중요!
─────────────────────────────────────────────────────────────────────────
  1. "병리 정보 저장 및 분석 시작" 버튼 클릭
  
  2. 다음 메시지들이 순서대로 나타나는지 확인:
     
     ⏳ "🔍 의사 소견서 GPT 파싱 중..."
        └─ GPT API 호출 중 (2-5초 소요)
     
     ✅ "의사 소견서 파싱 및 저장 완료! (Note ID: X)"
        └─ X는 숫자 (예: 3, 4, 5...)
     
     ✅ "병리 정보 저장 완료!"
  
  3. ❌ 에러 메시지가 나타나는 경우:
     - 스크린샷 캡처
     - 전체 에러 메시지 복사
     - 아래 "문제 발생 시" 참고


STEP 6: 데이터베이스 검증 (선택사항)
─────────────────────────────────────────────────────────────────────────
  터미널에서 다음 명령 실행:
  
  python -c "
from patient_management_system.database.db_enhanced import get_session
from patient_management_system.database.models_enhanced import PhysicianNote
import json

db = get_session()
notes = db.query(PhysicianNote).order_by(PhysicianNote.id.desc()).limit(5).all()

print('\\n최근 저장된 의사 소견서:')
for note in notes:
    print(f'\\nNote ID: {note.id}')
    print(f'Patient ID: {note.patient_id}')
    print(f'Physician: {note.physician_name}')
    print(f'Clinical Assessment: {note.clinical_assessment[:100]}...')
    print(f'Severity: {note.severity_score}/10')
    print(f'Symptoms: {json.loads(note.symptoms)}')
"

================================================================================
  ✅ 성공 기준
================================================================================

다음 모든 항목이 만족되어야 합니다:

□ 환자 검색 성공
□ 환자 선택 후 정보 표시됨
□ 의사 소견서 입력 가능
□ GPT 파싱 로딩 메시지 표시
□ "파싱 및 저장 완료" 성공 메시지 표시
□ Note ID가 숫자로 표시됨
□ 에러 메시지 없음

================================================================================
  ❌ 문제 발생 시 체크리스트
================================================================================

문제 1: "환자 DB ID를 찾을 수 없습니다"
  원인: 환자 데이터에 id 필드 없음
  해결: 
    1. 다른 환자로 시도
    2. 신규 환자 등록 후 테스트
    3. Backend 로그 확인 (API 응답 구조)

문제 2: "의사 소견서 저장 실패"
  원인: DB 연결 또는 GPT API 문제
  해결:
    1. Backend 터미널 로그 확인
    2. .env 파일에 OPENAI_API_KEY 확인
    3. DB 파일 권한 확인

문제 3: GPT 파싱이 끝나지 않음
  원인: OpenAI API 느림 또는 타임아웃
  해결:
    1. 1분 정도 기다려보기
    2. Backend 로그에서 API 호출 상태 확인
    3. Mock parser 사용 (OPENAI_API_KEY 제거)

================================================================================
  📊 테스트 결과 보고
================================================================================

테스트 완료 후 다음 정보를 알려주세요:

1. 모든 단계 성공했나요? (예/아니오)
2. Note ID가 정상적으로 표시되었나요?
3. 에러 메시지가 있었나요? (있다면 전체 메시지)
4. 데이터베이스 검증 결과 (선택사항)

================================================================================
  🎉 테스트 완료!
================================================================================

성공하면 Phase 1.2 COMPLETE! 🎊
다음: Phase 1.3 (Cell Image Upload & Analysis) 또는
      실제 DICOM 데이터로 E2E 테스트

================================================================================
""")
