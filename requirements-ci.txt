# ============================================================
# ADDS CI Requirements — Lightweight (no GPU, no heavy GUI)
# Used by GitHub Actions CI pipeline
# For full local development, use: pip install -r requirements.txt
# ============================================================

# Web Framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# Data Processing
numpy>=1.24.0
pandas>=2.1.0
scipy>=1.11.0

# Image Processing (lightweight)
pillow>=10.0.0
scikit-image>=0.21.0

# ML Core (CPU only — torch installed separately in CI)
scikit-learn>=1.3.0

# Database
sqlalchemy>=2.0.0

# API & Utils
openai>=1.0.0
python-dotenv>=1.0.0
pyyaml>=6.0
tqdm>=4.66.0
requests>=2.31.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0
httpx>=0.25.0

# Linting (CI checks)
ruff>=0.3.4
